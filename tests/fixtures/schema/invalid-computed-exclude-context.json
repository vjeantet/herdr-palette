{
  "schema_version": 1,
  "expected_herdr_protocol": 20,
  "commands": [
    {
      "id": "invalid.computed.exclude",
      "title": "Invalid computed exclusion",
      "description": "Computed context must not filter a selector.",
      "command": ["workspace", "focus"],
      "arguments": [
        {
          "source": "select",
          "selector": "workspaces",
          "prompt": "workspace > ",
          "exclude_context": "next_workspace_id"
        }
      ]
    }
  ]
}
